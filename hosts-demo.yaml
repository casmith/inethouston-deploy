all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
  children:
    servers:
      hosts:
        demo:
          ansible_host: 192.168.1.100  # Replace with your demo VM's IP address
          ansible_user: ubuntu
          deploy_path: /home/ubuntu/deploy
